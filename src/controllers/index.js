const logControllers = require('./logs');
const rootControllers = require('./root');
const userControllers = require('./user');

module.exports = {
  logControllers,
  rootControllers,
  userControllers,
};
